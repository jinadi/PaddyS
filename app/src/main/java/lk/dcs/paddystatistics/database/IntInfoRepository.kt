package lk.dcs.paddystatistics.database

class IntInfoRepository(private val db:MyDatabase) {

    /*suspend fun insertIntInfo(intInfo: IntInfo) = db.getIntInfoDao().insertIntInfo(intInfo)
    suspend fun deleteIntInfo(intInfo: IntInfo) = db.getIntInfoDao().deleteIntInfo(intInfo)*/
}